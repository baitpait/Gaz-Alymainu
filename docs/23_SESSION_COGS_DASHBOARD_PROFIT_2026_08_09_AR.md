# جلسة 2026-08-09 — COGS، حذف مصروفات، أرباح اليوم على اللوحة

> **الغرض:** توثيق ما أُنجز في جلسة 22 بعد افتتاحها.
> Commits: `f97eb36` (COGS) → `b28c1da` (حذف مصروف) → `48626a4` (أرباح اليوم).

---

## 1) ملخص تنفيذي

| البند | Commit | حالة الإنتاج |
|--------|--------|----------------|
| P&L = مبيعات − تكلفة المباع؛ أصل مخزون منفصل؛ استبعاد تقييم المخزون | `f97eb36` | ✅ + migrate |
| حذف مصروف سائق (محاسب) يعيد الرصيد | `b28c1da` | ✅ |
| بطاقة أرباح اليوم على `/dashboard` | `48626a4` | ✅ |
| توثيق جلسات 21–22 | ضمن الدفعات أعلاه | ✅ |

---

## 2) معادلة الربح (مرجع ثابت)

```text
صافي = مبيعات الفترة − COGS − مصروفات − رواتب
COGS = Σ (كمية البيع غير الملغى × تكلفة الصنف ILS)
أصل المخزون = Σ (رصيد حالي × تكلفة)   ← خارج الصافي
مشتريات تشغيلية = فواتير issued غير تقييم   ← مرجع فقط
```

**أرباح اليوم (لوحة التحكم):** نفس المعادلة لـ `date_from = date_to = اليوم` عبر `ProfitLossReportService` (وضع كامل).

---

## 3) تفاصيل تقنية

### COGS / تقييم المخزون
- عمود `purchase_orders.is_inventory_valuation` (migrate `2026_08_09_131711_…`).
- تمييز تلقائي للملاحظات التي تبدأ بـ `تقييم مخزون`.
- واجهة P&L: «تكلفة المباع» + بطاقة أصل المخزون.
- توثيق المعادلات: `docs/11_PROFIT_AND_LOSS_AR.md`.

### حذف مصروف سائق
- Gate: `delete-driver-expenses` = محاسب/مدير.
- Soft-delete → `CashBoxService::balance` يرتفع تلقائياً.
- السائق لا يحذف.

### أرباح اليوم
- `DashboardSummaryService::todayOps` يستدعي P&L ليوم واحد.
- حقول: `profit`, `cogs`, `expenses_total`, `salaries`, `pnl_sales`.
- البطاقة الأولى في صف «اليوم»؛ رابط إلى تقرير الربح بفلتر اليوم.

---

## 4) نشر الإنتاج (نفّذ)

```bash
git pull origin main
php artisan migrate --force   # مطلوب لـ f97eb36 مرة واحدة
php artisan optimize:clear && php artisan view:cache
```

لا migrate إضافي لـ `b28c1da` / `48626a4`.

---

## 5) بقايا ما زالت معلّقة

1. أصناف «شراء فقط» — إخفاء من POS / علم قابل للبيع.
2. فجوة سيارة أيهم / جرة 48 (−1) إن لم تُسوَّ بجرد.
3. فاتورة التقييم على الإنتاج — التحقق إن أُنشئت بأمر tinker سابقاً.

---

## 6) مراجع

- `docs/11_PROFIT_AND_LOSS_AR.md`
- `docs/14_GAS_DRIVER_INVENTORY_SYSTEM_AR.md`
- `docs/22_SESSION_KICKOFF_2026_08_09_AR.md`
- `docs/PROJECT_LOG.md`
